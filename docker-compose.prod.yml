version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: production
    container_name: searge-frontend-prod
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - searge-network

networks:
  searge-network:
    driver: bridge
